# Reference: https://github.com/i3/i3/blob/next/etc/config

# Config version for compatibility and deprecations
# See: https://nikitabobko.github.io/AeroSpace/guide#config-version
config-version = 2

# Start aerospace at login
start-at-login = true
# Automatically reload the config when this file is saved
auto-reload-config = true
# i3 doesn't have "normalizations" feature that why we disable them here.
# But the feature is very helpful.
# Normalizations eliminate all sorts of weird tree configurations that don't make sense.
# Give normalizations a chance and enable them back.
enable-normalization-flatten-containers = false
enable-normalization-opposite-orientation-for-nested-containers = false

# Mouse follows focus when focused monitor changes
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Keep the configured workspaces available even when they are empty.
persistent-workspaces = ['1', '2', '3', '4', '5', '6', '7', '8', '9']

[mode.main.binding]
    # See: https://nikitabobko.github.io/AeroSpace/goodies#open-a-new-window-with-applescript
    alt-enter = '''exec-and-forget osascript -e '
    tell application "alacritty"
        do script
        activate
    end tell'
    '''

    # i3 wraps focus by default
    alt-h =         'focus --boundaries-action wrap-around-the-workspace left'
    alt-j =         'focus --boundaries-action wrap-around-the-workspace down'
    alt-k =         'focus --boundaries-action wrap-around-the-workspace up'
    alt-l =         'focus --boundaries-action wrap-around-the-workspace right'

    alt-shift-h = 'move left'
    alt-shift-j = 'move down'
    alt-shift-k = 'move up'
    alt-shift-l=  'move right'

    # Consider using 'join-with' command as a 'split' replacement if you want to enable
    # normalizations
    # alt-h = 'split horizontal'
    # alt-v = 'split vertical'

    alt-f = 'fullscreen'
    alt-m = 'macos-native-fullscreen off'
    alt-shift-m = 'macos-native-minimize'

    alt-s = 'layout v_accordion' # 'layout stacking' in i3
    alt-w = 'layout h_accordion' # 'layout tabbed' in i3
    alt-e = 'layout tiles horizontal vertical' # 'layout toggle split' in i3

    alt-shift-space = 'layout floating tiling' # 'floating toggle' in i3

    # Not supported, because this command is redundant in AeroSpace mental model.
    # See: https://nikitabobko.github.io/AeroSpace/guide#floating-windows
    #alt-space = 'focus toggle_tiling_floating'

    # `focus parent`/`focus child` are not yet supported, and it's not clear whether they
    # should be supported at all https://github.com/nikitabobko/AeroSpace/issues/5
    # alt-a = 'focus parent'

    alt-1 = 'workspace 1'
    alt-2 = 'workspace 2'
    alt-3 = 'workspace 3'
    alt-4 = 'workspace 4'
    alt-5 = 'workspace 5'
    alt-6 = 'workspace 6'
    alt-7 = 'workspace 7'
    alt-8 = 'workspace 8'
    alt-9 = 'workspace 9'

    alt-shift-1 = 'move-node-to-workspace 1'
    alt-shift-2 = 'move-node-to-workspace 2'
    alt-shift-3 = 'move-node-to-workspace 3'
    alt-shift-4 = 'move-node-to-workspace 4'
    alt-shift-5 = 'move-node-to-workspace 5'
    alt-shift-6 = 'move-node-to-workspace 6'
    alt-shift-7 = 'move-node-to-workspace 7'
    alt-shift-8 = 'move-node-to-workspace 8'
    alt-shift-9 = 'move-node-to-workspace 9'



    alt-shift-c = 'reload-config'

    alt-r = 'mode resize'

    alt-minus = 'resize width -50'
    alt-equal = 'resize width +50'

[workspace-to-monitor-force-assignment]
    1 = 'main'
    2 = 'main'
    3 = 'main'
    4 = 'main'
    5 = 'main'
    6 = ['secondary', 'main']
    7 = ['secondary', 'main']
    8 = ['secondary', 'main']
    9 = ['secondary', 'main']

[mode.resize.binding]
    h = 'resize width -50'
    j = 'resize height +50'
    k = 'resize height -50'
    l = 'resize width +50'
    enter = 'mode main'
    esc = 'mode main'


[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.anthropic.claudefordesktop'
    run = 'move-node-to-workspace 4'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.AppStore'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.ActivityMonitor'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.calculator'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.finder'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.iCal'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.finder'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.apple.iphonesimulator'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.1password.1password'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.bitwarden.desktop'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.google.android.studio'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.microsoft.Outlook'
    run = 'move-node-to-workspace 3'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.microsoft.teams2'
    run = 'move-node-to-workspace 4'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.microsoft.VSCode'
    run = 'move-node-to-workspace 3'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.whoosh.whooshgame'
    run = ['macos-native-fullscreen off']

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.postmanlabs.mac'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.tinyspeck.slackmacgap'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.spotify.client'
    run = ['layout floating', 'move-node-to-workspace 1']

[[on-window-detected]]
    if = 'test %{app-bundle-id} = com.cisco.secureclient.gui'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = md.obsidian'
    run = ['move-node-to-workspace 3']

[[on-window-detected]]
    if = 'test %{app-bundle-id} = org.alacritty'
    run = ['move-node-to-workspace 2']

[[on-window-detected]]
    if = 'test %{app-bundle-id} = net.whatsapp.WhatsApp'
    run = 'layout floating'

[[on-window-detected]]
    if = 'test %{app-bundle-id} = fr.my-monkey.opensuperwhisper'
    run = 'layout floating'
